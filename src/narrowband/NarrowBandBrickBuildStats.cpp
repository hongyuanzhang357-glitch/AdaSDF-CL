#include "adasdf/narrowband/NarrowBandBrickBuildStats.h"

namespace adasdf {
namespace {
static_assert(sizeof(NarrowBandBrickBuildStats) > 0,
              "NarrowBandBrickBuildStats must be complete");
}  // namespace
}  // namespace adasdf
