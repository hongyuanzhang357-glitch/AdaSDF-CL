#include "adasdf/narrowband/CompressionBrick.h"

namespace adasdf {
namespace {
static_assert(sizeof(CompressionBrick) > 0,
              "CompressionBrick must be complete");
}  // namespace
}  // namespace adasdf
